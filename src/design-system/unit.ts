/**
 * Перевод «пикселя макета» (база 440px ширины листа) во флюид-cqw.
 * u(48) -> "calc(48 * var(--u))". Используй для инлайновых размеров в TSX,
 * чтобы элемент масштабировался вместе с листом, как в исходном лендинге.
 */
export const u = (n: number): string => `calc(${n} * var(--u))`;
